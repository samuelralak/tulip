= simple_form_for([locals[:petty_cash], locals[:petty_cash_item]]) do |f|
  = f.error_notification

  .form-inputs
    = f.input :amount
    = f.input :paid_to
    = f.input :reason
    = f.input :date
    = f.input :bal_carried_forward

  .form-actions
    = f.button :submit
